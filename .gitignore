# Never commit the personal build or the sanitizer (both contain private data)
vivian_toolkit*.html
sanitize.py
harness*.html
*.part

# Private financial model — never commit
model.html
model*.html
nyc budgeting*.xlsx
